{
  "type": "module",
  "engines": {
    "node": ">=20.6.0"
  },
  "dependencies": {
    "nanoid": "^5.0.1",
    "wallpaper": "^7.0.0"
  },
  "scripts": {
    "start": "node --env-file=.env dist/index.js",
    "build": "rimraf ./dist && tsc"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "rimraf": "^5.0.5"
  }
}
